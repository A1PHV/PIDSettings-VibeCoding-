# Продвинутые методы настройки PID

## Обзор

Программа теперь включает профессиональные алгоритмы идентификации систем и настройки PID, которые подходят даже для сложных нестандартных систем.

---

## 🎯 Рекомендации по выбору метода

### **AUTO** (РЕКОМЕНДУЕТСЯ)
```bash
pid_tuner.exe analyze -i flight.bin -m auto
```
- Автоматически выбирает наилучший метод
- Тестирует несколько подходов
- Учитывает требования к системе
- **Используйте когда:** не уверены какой метод выбрать

---

## 📚 Доступные методы

### 1. **IMC (Internal Model Control)**
```bash
pid_tuner.exe analyze -i flight.bin -m imc
```

**Когда использовать:**
- ✅ Системы с заметной задержкой (dead time)
- ✅ Требуется высокая робастность
- ✅ Неопределенность в параметрах системы
- ✅ Хорошо для большинства БПЛА

**Преимущества:**
- Очень робастный метод
- Гарантирует стабильность
- Простая настройка агрессивности

**Параметры:**
- λ (lambda) = Желаемая скорость отклика
- Больше λ = медленнее, но стабильнее

**Теория:**
```
Для FOPDT модели G(s) = K*exp(-θs)/(τs+1):

Kp = τ / (K*(λ + θ))
Ki = 1 / τ
Kd = 0
```

---

### 2. **SIMC (Skogestad IMC)**
```bash
pid_tuner.exe analyze -i flight.bin -m simc
```

**Когда использовать:**
- ✅ Оптимизация подавления возмущений
- ✅ Системы с интегрирующим поведением
- ✅ Промышленные приложения
- ✅ Нужен баланс между скоростью и стабильностью

**Преимущества:**
- Оптимизирован для disturbance rejection
- Хороший компромисс производительности
- Консервативная настройка

**Отличия от IMC:**
- Более агрессивный по I
- Лучше справляется с внешними возмущениями

---

### 3. **Cohen-Coon**
```bash
pid_tuner.exe analyze -i flight.bin -m cohen-coon
```

**Когда использовать:**
- ✅ Большое соотношение θ/τ (dead time / time constant > 0.3)
- ✅ Процессы с преобладающей задержкой
- ✅ Медленные системы

**Преимущества:**
- Лучше чем Z-N для систем с большой задержкой
- Учитывает соотношение θ/τ

**Формулы:**
```
R = θ/τ (dead time ratio)

Kp = (1/K) * (τ/θ) * (1.35 + 0.27*R) / (1 + 0.6*R)
Ti = θ * (2.5 - 2*R) / (1 - 0.39*R)
Td = θ * 0.37 / (1 - 0.81*R)
```

---

### 4. **Lambda Tuning**
```bash
pid_tuner.exe analyze -i flight.bin -m lambda
```

**Когда использовать:**
- ✅ Нужен явный контроль скорости отклика
- ✅ Настройка агрессивности критична
- ✅ Требуется предсказуемое поведение

**Преимущества:**
- Интуитивный параметр настройки (λ)
- Связь между λ и временем отклика
- Хорошая стабильность

**Параметр λ:**
- λ = 0.5 * θ → быстрый отклик
- λ = 1.0 * θ → средний
- λ = 2.0 * θ → медленный, стабильный

---

### 5. **Tyreus-Luyben**
```bash
pid_tuner.exe analyze -i flight.bin -m tyreus-luyben
```

**Когда использовать:**
- ✅ Высокие требования к стабильности
- ✅ Критические системы
- ✅ Неизвестная динамика
- ✅ "Лучше медленно, но надежно"

**Преимущества:**
- Более консервативен чем Ziegler-Nichols
- Меньше перерегулирование
- Выше запасы устойчивости

**Формулы:**
```
Kp = Ku / 3.2
Ti = 2.2 * Tu
Td = Tu / 6.3
```

---

### 6. **Optimization**
```bash
pid_tuner.exe analyze -i flight.bin -m optimization
```

**Когда использовать:**
- ✅ Нужна максимальная производительность
- ✅ Известны требования к системе
- ✅ Готовы потратить время на расчет
- ✅ Нестандартные критерии качества

**Преимущества:**
- Оптимизирует конкретные критерии
- IAE, ISE, ITAE, ITSE
- Учитывает ограничения

**Критерии оптимизации:**
- **IAE** (Integral Absolute Error) - общий
- **ISE** (Integral Square Error) - штрафует большие ошибки
- **ITAE** (Integral Time Absolute Error) - быстрый отклик
- **ITSE** (Integral Time Square Error) - компромисс

---

### 7. **Frequency Domain**
```bash
pid_tuner.exe analyze -i flight.bin -m frequency
```

**Когда использовать:**
- ✅ Критичны запасы устойчивости
- ✅ Частотные характеристики известны
- ✅ Требования по phase/gain margin
- ✅ Анализ в частотной области

**Преимущества:**
- Гарантирует заданные запасы устойчивости
- Учитывает частотные характеристики
- Хорошо для систем с резонансами

**Параметры:**
- Phase margin (φm) = 40-60° (типично 60°)
- Gain margin (Gm) = 6-12 dB (типично 10 dB)

---

### 8. **Ziegler-Nichols** (классический)
```bash
pid_tuner.exe analyze -i flight.bin -m ziegler-nichols
```

**Когда использовать:**
- ✅ Быстрая оценочная настройка
- ✅ Совместимость со старыми методами
- ✅ Обратная совместимость

**Недостатки:**
- Агрессивная настройка
- Часто требует уменьшения коэффициентов
- Не учитывает специфику системы

---

### 9. **Relay Method**
```bash
pid_tuner.exe analyze -i flight.bin -m relay
```

**Когда использовать:**
- ✅ Метод Åström-Hägglund
- ✅ Обнаружение limit cycles
- ✅ Более консервативен чем Z-N

---

## 🔬 Идентификация системы

Программа автоматически идентифицирует модель системы используя:

### 1. **ARX (AutoRegressive eXogenous)**
- Параметрическая идентификация
- Модель: A(q)y(t) = B(q)u(t) + e(t)
- Быстро, эффективно

### 2. **FOPDT (First Order Plus Dead Time)**
- G(s) = K*exp(-θs)/(τs+1)
- Три параметра: K (gain), τ (time constant), θ (dead time)
- Наиболее распространенная модель

### 3. **Second-Order System**
- G(s) = K*ωn²/(s² + 2ζωns + ωn²)
- Параметры: ζ (damping), ωn (natural frequency)
- Для колебательных систем

### 4. **Нелинейная оптимизация**
- Минимизация ошибки модели
- L-BFGS-B алгоритм
- Наиболее точная идентификация

---

## 📊 Обработка сигналов

Автоматическая предобработка включает:

### 1. **Фильтрация**
- Удаление выбросов (MAD-based robust method)
- Savitzky-Golay фильтр (сохраняет форму)
- Kalman фильтр (опционально)
- Butterworth фильтр

### 2. **Ресэмплинг**
- Равномерная частота дискретизации
- Интерполяция пропущенных значений
- Cubic spline интерполяция

### 3. **Detrending**
- Удаление DC offset
- Удаление линейного тренда
- Нормализация

### 4. **Сегментация**
- Обнаружение активных участков полета
- Moving variance analysis
- Автоматический выбор лучшего сегмента

---

## 📈 Метрики качества

Программа вычисляет и показывает:

### Временные характеристики:
- **Rise Time** - время нарастания (10% → 90%)
- **Settling Time** - время установления (± 2%)
- **Overshoot** - перерегулирование (%)
- **Steady-State Error** - установившаяся ошибка

### Частотные характеристики:
- **Phase Margin** - запас по фазе (градусы)
- **Gain Margin** - запас по усилению (дB)
- **Crossover Frequency** - частота среза

### Интегральные критерии:
- **IAE** - Integral Absolute Error
- **ISE** - Integral Square Error
- **ITAE** - Integral Time Absolute Error

### Качество модели:
- **R²** - коэффициент детерминации
- **MSE** - Mean Squared Error
- **Fit %** - процент совпадения модели

---

## 🎓 Сравнительная таблица методов

| Метод | Скорость | Робастность | Сложность | Для новичков |
|-------|----------|-------------|-----------|--------------|
| AUTO | ⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ | ✅ ДА |
| IMC | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐ | ✅ ДА |
| SIMC | ⭐⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐ | ✅ ДА |
| Cohen-Coon | ⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐ | ⚠️ Средне |
| Lambda | ⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐ | ✅ ДА |
| Tyreus-Luyben | ⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐ | ✅ ДА |
| Optimization | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ❌ НЕТ |
| Frequency | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ | ❌ НЕТ |
| Z-N | ⭐⭐⭐⭐⭐ | ⭐⭐ | ⭐ | ⚠️ Требует корректировки |

---

## 💡 Практические рекомендации

### Для начинающих:
1. Начните с `auto`
2. Если результат не устраивает - попробуйте `imc`
3. Для более стабильного полета - `tyreus-luyben`

### Для опытных пилотов:
1. `simc` для лучшего подавления возмущений
2. `optimization` для максимальной производительности
3. `frequency` для систем с известными резонансами

### Для нестандартных систем:
1. Сначала `auto` для базового решения
2. `cohen-coon` если большая задержка
3. `optimization` с custom constraints
4. Анализ частотных характеристик с `frequency`

---

## 🔧 Примеры использования

### Пример 1: Типичный квадрокоптер
```bash
# Auto-tune (рекомендуется)
pid_tuner.exe analyze -i flight.bin -m auto

# Результат покажет автоматически выбранный метод
# и качество настройки
```

### Пример 2: Тяжелый БПЛА с инерцией
```bash
# IMC для робастности
pid_tuner.exe analyze -i heavy_drone.bin -m imc

# Или Tyreus-Luyben для стабильности
pid_tuner.exe analyze -i heavy_drone.bin -m tyreus-luyben
```

### Пример 3: Гоночный квадрокоптер
```bash
# Оптимизация для скорости
pid_tuner.exe analyze -i racing.bin -m optimization -a roll

# Или SIMC для баланса
pid_tuner.exe analyze -i racing.bin -m simc
```

### Пример 4: Система с известными проблемами
```bash
# Frequency domain для анализа
pid_tuner.exe analyze -i problem_log.bin -m frequency

# Посмотрите на Phase/Gain margins в выводе
```

---

## 📚 Дополнительная информация

### Книги:
1. Åström & Murray - "Feedback Systems"
2. Skogestad & Postlethwaite - "Multivariable Feedback Control"
3. Franklin et al. - "Feedback Control of Dynamic Systems"

### Статьи:
1. Ziegler & Nichols (1942) - "Optimum Settings for Automatic Controllers"
2. Åström & Hägglund (1984) - "Automatic Tuning of Simple Regulators"
3. Skogestad (2003) - "Simple Analytic Rules for Model Reduction"

### Online ресурсы:
- Control Tutorials for MATLAB
- Brian Douglas YouTube Channel
- Ardupilot Tuning Guide

---

Созданно с использованием профессиональных алгоритмов идентификации систем и настройки управления.
